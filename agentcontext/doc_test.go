package agentcontext

import "testing"

// TestPackagePresent is a placeholder so `go test ./...` exits 0 against
// the otherwise-empty agentcontext package while the sibling Phase 2
// commit fills in the ContextRequest / ContextResult / SlotSource
// surface.
func TestPackagePresent(t *testing.T) {
	t.Log("agentcontext package compiles")
}
